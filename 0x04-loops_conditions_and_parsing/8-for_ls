#!/usr/bin/env bash
# This script lists all non-hidden files, showing only the part after the first dash if present

for file in *; do
  # Check if the filename contains a dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  else
    echo "$file"
  fi
done
